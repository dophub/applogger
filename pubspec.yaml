name: app_logger
description: Log all Http request and Navigation.
version: 0.0.7

environment:
  sdk: '>=2.17.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  background_json_parser: any
  dio: ^5.9.2
  device_info_plus: ^13.1.0
  package_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter: